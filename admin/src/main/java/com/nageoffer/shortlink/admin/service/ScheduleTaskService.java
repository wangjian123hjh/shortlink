package com.nageoffer.shortlink.admin.service;

//@Service
//@Slf4j
//@RequiredArgsConstructor
public class ScheduleTaskService {
//    private final UserService userService;
//    private final RBloomFilter userRegisterCachePenetrationBloomFilter;

    public void executeStartupTask(){
//        LambdaQueryWrapper<UserDO> queryWrapper = Wrappers.lambdaQuery(UserDO.class)
//                .select(UserDO::getUsername);
//        List<UserDO> list = userService.list(queryWrapper);
//        log.debug("正在执行将用户名刷入布隆过滤器");
//        for (UserDO user : list){
//            log.debug("正在刷入的用户名为:{}",user.getUsername());
//            userRegisterCachePenetrationBloomFilter.add(user.getUsername());
//        }
    }
}
